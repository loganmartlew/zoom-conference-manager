import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    secondary: {
      light: '#00e08b',
      main: '#02a165',
      contrastText: '#000000',
    },
  },
});

export default theme;
