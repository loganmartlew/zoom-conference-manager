// For using in Query-string parameter
export enum ZoomMeetingType {
  SCHEDULED = 'scheduled',
  LIVE = 'live',
  UPCOMING = 'upcoming',
  UPCOMING_MEETINGS = 'upcoming_meetings',
  PREVIOUS_MEETINGS = 'upcoming_meetings',
}
